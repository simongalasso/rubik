version: "3"
services:
  rubik-server:
    build:
      context: .
      dockerfile: .docker/rubik-server/Dockerfile
    container_name: rubik-server
    ports:
      - "8080:8080"
    restart: always
    volumes:
      - ./server/src/public:/rubik-server/public
